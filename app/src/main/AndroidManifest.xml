<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nilearning.weatherin">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".WeatherInApplication"
        android:allowBackup="true"
        android:icon="@drawable/cloudy_day_fog_foggy_mist_sun_weather"
        android:label="@string/app_name"
        android:roundIcon="@drawable/cloudy_day_fog_foggy_mist_sun_weather"
        android:supportsRtl="true"
        android:theme="@style/Theme.WeatherIN">
        <activity
            android:name=".ui.WeatherinActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>